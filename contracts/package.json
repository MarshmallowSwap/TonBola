{
  "name": "tonbola-contract",
  "version": "1.0.0",
  "scripts": {
    "build": "blueprint build",
    "test": "blueprint test",
    "deploy:testnet": "blueprint run --network testnet",
    "deploy:mainnet": "blueprint run --network mainnet",
    "gen-oracle-key": "ts-node scripts/deploy.ts --gen-oracle-key"
  },
  "dependencies": {
    "@tact-lang/compiler": "^1.6.13",
    "@ton/core": "^0.63.1",
    "@ton/crypto": "^3.3.0",
    "@ton/ton": "^15.4.0"
  },
  "devDependencies": {
    "@ton/blueprint": "^0.20.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
